const handleLogout = () => {
  // Perform logout logic, remove authentication from localStorage, etc.
  localStorage.setItem("isLoggedIn", "false");
  // Perform any other cleanup or redirection logic as needed
};
